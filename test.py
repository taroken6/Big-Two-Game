import pygame
import pygame.gfxdraw
from card import Card
from deck import Deck
import ast
import string
import math
from game import Game
import json

# width = 800
# height = 800
# window = pygame.display.set_mode((width, height))
# pygame.display.set_caption("Big Two")
#
# red = (255, 0, 0)
# white = (255, 255, 255)
# green = (0, 150, 55)
#
# run = True
# clock = pygame.time.Clock()
# window.fill(green)
# pygame.display.update()

test = '[0,1,2]'
print(str(["Yes",2]))
# test = (list(int(x) for x in test))
# print(test)

# while run:
#     clock.tick(60)
#
#     pygame.draw.rect(window, (255, 0, 0, 255),  (80, 80, 100, 100))
#     pygame.gfxdraw.box(window, pygame.Rect(260,260,100,100), (100,0,0,0))
#
#
#     for event in pygame.event.get():
#         if event.type == pygame.QUIT:
#             run = False
#             pygame.quit()
#
#     pygame.display.update()

